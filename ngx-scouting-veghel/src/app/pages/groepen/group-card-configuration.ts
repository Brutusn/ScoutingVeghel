import { GenderIcons } from '@interfaces';

export interface GroupCardConfiguration {
  title: string;
  genderIcon: GenderIcons;
  content: string;
  img: string;
  age: string;
  meetingTime: string;
}
